<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_gravity="center"
    android:orientation="vertical"
    android:paddingLeft="20dp"
    android:paddingRight="20dp"
    tools:context=".MainActivity">


    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <include layout="@layout/id_register_item" />

        <Button
            android:id="@+id/plus_id_register"

            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:onClick="onClickRegisterBtn"
            android:text="@string/register_game_id" />

        <Button
            android:id="@+id/matchingBtn"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:onClick="onClickMatchingBtn"
            android:text="@string/matching_friend" />
        <Button
            android:id="@+id/chatBtn"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:onClick="onClickChattingBtn"
            android:text="@string/matching_friend" />

        <Button
            android:id="@+id/friendBtn"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:onClick="onClickFriendBtn"
            android:text="Button" />
            android:text="@string/register_game_id"/>

    </LinearLayout>

</LinearLayout>